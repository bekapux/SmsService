import { SmsTemplateTextPipePipe } from './sms-template-text-pipe.pipe';

describe('SmsTemplateTextPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SmsTemplateTextPipePipe();
    expect(pipe).toBeTruthy();
  });
});
