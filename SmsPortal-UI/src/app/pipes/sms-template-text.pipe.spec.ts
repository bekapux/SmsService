import { SmsTemplateTextPipe } from './sms-template-text.pipe';

describe('SmsTemplateTextPipe', () => {
  it('create an instance', () => {
    const pipe = new SmsTemplateTextPipe();
    expect(pipe).toBeTruthy();
  });
});
